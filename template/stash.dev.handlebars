name: "{{@package 'displayName'}} β"
desc: |-
  BETA:
  {{#each (split (@package 'description') "\n")}}
  {{{this}}}
  {{/each}}
openUrl: "{{@package 'openUrl'}}.beta"
author: |-
  {{#each (@package 'contributors')}}
  {{this}}
  {{/each}}
homepage: "{{@package 'homepage'}}"
icon: "{{@package 'icon'}}"
category: "{{@package 'organizationName'}}"
date: "{{now "yyyy-MM-dd HH:mm:ss"}}"
version: "dev"

rules:
  # 🗺️ Amap
  - DOMAIN-SUFFIX,is.autonavi.com,DIRECT
  # 🗺️ Maps
  - DOMAIN,gspe11-2-cn-ssl.ls.apple.com,DIRECT
  - DOMAIN,gspe12-cn-ssl.ls.apple.com,DIRECT
  - DOMAIN,gspe19-cn-ssl.ls.apple.com,DIRECT
  - DOMAIN,gspe19-2-cn-ssl.ls.apple.com,DIRECT
  - DOMAIN,gspe79-cn-ssl.ls.apple.com,DIRECT

http:
  mitm:
    - "configuration.ls.apple.com"
    - "gspe35-ssl.ls.apple.com"
  script:
    - match: ^https?:\/\/configuration\.ls\.apple\.com\/config\/defaults
      name: GeoServices.request
      type: request
      argument:
    - match: ^https?:\/\/configuration\.ls\.apple\.com\/config\/defaults
      name: GeoServices.response
      type: response
      require-body: true
      argument:
    - match: ^https?:\/\/gspe35-ssl\.ls\.apple\.(com|cn)\/config\/announcements
      name: GeoServices.request
      type: request
      argument:
    - match: ^https?:\/\/gspe35-ssl\.ls\.apple\.(com|cn)\/config\/announcements
      name: GeoServices.response
      type: response
      require-body: true
      binary-mode: true
      argument:
    - match: ^https?:\/\/gspe35-ssl\.ls\.apple\.(com|cn)\/geo_manifest\/dynamic\/config
      name: GeoServices.request
      type: request
      argument:
    - match: ^https?:\/\/gspe35-ssl\.ls\.apple\.(com|cn)\/geo_manifest\/dynamic\/config
      name: GeoServices.response
      type: response
      require-body: true
      binary-mode: true
      argument:

script-providers:
  GeoServices.request:
    url: https://gist.githubusercontent.com/VirgilClyne/b7524906119fa250bc933cc9a08e939a/raw/request.bundle.js
    interval: 86400
  GeoServices.response:
    url: https://gist.githubusercontent.com/VirgilClyne/b7524906119fa250bc933cc9a08e939a/raw/response.bundle.js
    interval: 86400
